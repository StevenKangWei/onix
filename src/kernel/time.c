#include <onix/time.h>

void sleep(int milliseconds)
{
    if (milliseconds <= 0)
        return;
    while (milliseconds--)
    {
    }
}

void delay(int milliseconds)
{
    if (milliseconds <= 0)
        return;
    while (milliseconds--)
    {
    }
}